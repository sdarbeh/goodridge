import styled from 'styled-components'

export const ActiveSvg = styled.svg`
    color: ${props => props.theme.currentThemeColor};
`

export const ThemeMoji = styled.img`
    border-radius: 50%;
    overflow: hidden;
`